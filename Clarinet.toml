[project]
name = "fetch-delegator"
authors = ["Web3 Delegation Team"]
description = "A decentralized metric delegation and tracking protocol"
telemetry = false
requirements = []
[contracts.metric-delegator]
path = "contracts/metric-delegator.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
